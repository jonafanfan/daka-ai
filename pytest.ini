[pytest]
# Put the project root on sys.path so `pytest` works the same as `python -m pytest`.
# Without this, bare `pytest` can't import scene_analysis / api_server.
pythonpath = .
testpaths = tests
addopts = -q
